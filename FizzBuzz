/**
 * @param {number} n
 * @return {string[]}
 */
var fizzBuzz = function(n) {

    let i=1;
    let o = [];

    while (i <= n) {

        if(i%3 === 0 && i%5==0) {
            o.push('FizzBuzz');
        }
        else if(i%3 == 0) {
            o.push('Fizz');
        }
        else if(i%5 === 0) {
            o.push('Buzz');
        }
        else {
            o.push(i.toString());
        }

        i++;
    }

    console.log(o);
    
};

fizzBuzz(20);